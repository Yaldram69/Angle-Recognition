# Angle-Recognition
Real-time object angle recognition system built with OpenCV, PyTorch, and Mask R-CNN that detects objects from a webcam feed, and computes each object’s rotation angle relative to the horizontal axis. The system draws a rotated bounding box around detected objects and displays the estimated angle in degrees on the live video stream.
